#!/bin/bash

# Global config
SERVICE_NAME=stackdriver-notifs
SERVICE_REGION=us-central1
SA_NAME=notif-service
TOPIC_NAME=mocked
IMAGE_VERSION="0.1.4"

# GCP metadata
PROJECT=$(gcloud config get-value project)
PROJECT_NUM=$(gcloud projects describe ${PROJECT} --format="value(projectNumber)")

